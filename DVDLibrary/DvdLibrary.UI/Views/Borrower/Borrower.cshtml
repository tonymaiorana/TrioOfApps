@model List<DvdLibrary.Models.Borrower>
@{
    ViewBag.Title = "BorrowerData";
}

<h2>Borrower Data</h2>

    List of Borrowers / Information
    may be only viewable if admin.
<table class="table table-striped table-hover ">
    <thead>
        <tr>
            <th>Borrower ID</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Phone Number</th>
            <th>Borrow History</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var borrower in Model)
            {
            <tr>
                <td>@borrower.BorrowerId</td>
                <td>@borrower.FirstName</td>
                <td>@borrower.LastName</td>
                <td>@borrower.PhoneNumber</td>
                <td>@Html.ActionLink("View Details", "BorrowHistory", "Borrower", new { id = borrower.BorrowerId }, new { @class = "btn btn-default" })</td>
            </tr>
        }
    </tbody>
</table> 